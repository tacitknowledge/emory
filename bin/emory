#!/usr/bin/env ruby

require 'logging'
require 'emory/runner'

Logging.logger.root.add_appenders(Logging.appenders.stdout(level: :info),
                                  Logging.appenders.rolling_file('emory.log', level: :debug))

Emory::Runner.start
