require 'spec_helper'

module Emory
  
  describe ConfigurationFile do
    context "#read" do
      before(:each) do
        #TODO: executes before each 'it' if necessary
      end
      it 'should raise error if no configuration file found' do
        #TODO: add test code here
      end
      it 'should find configuration file in current directory'
      it 'should find configuration file in parent direcory'
      it 'should find configuration file in root directory'
    end
  end

end